// This is Component That can be resused any where
// so component are the resuable UI code that are rendered on application
function MyButton(){
    return <button>Click Me</button>
}

export default MyButton;